# ═══════════════════════════════════════════════════════════════
# CatVTON Virtual Try-On – WSL Requirements
# ═══════════════════════════════════════════════════════════════
# PyTorch is installed separately via: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# ── CatVTON Core ──────────────────────────────────────────────
accelerate==0.31.0
diffusers
transformers
numpy==1.26.4
opencv-python==4.10.0.84
pillow
scikit-image
scipy
tqdm
peft
huggingface_hub
hf_transfer
matplotlib

# ── DensePose / Detectron2 deps ───────────────────────────────
fvcore
cloudpickle
omegaconf
pycocotools

# ── Server ────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart
python-dotenv
pydantic-settings
boto3
aiofiles
