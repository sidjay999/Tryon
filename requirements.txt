# ── Core ───────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
pydantic-settings>=2.2.0

# ── NumPy – must be <2 for PyTorch 2.2.0 ───────────────
numpy>=1.26.0,<2.0.0

# ── PyTorch – installed separately via install.ps1 ──────
# torch==2.2.0+cu121
# torchvision==0.17.0+cu121

# ── Diffusion / ML – pinned for torch 2.2.0 ────────────
diffusers==0.27.2
transformers==4.40.0
accelerate>=0.28.0
controlnet-aux>=0.0.7
huggingface-hub>=0.21.0

# ── Identity (future – requires C++ Build Tools) ────────
# insightface>=0.7.3
# onnxruntime-gpu>=1.17.0

# ── Computer Vision ─────────────────────────────────────
opencv-python-headless>=4.9.0.80
Pillow>=10.2.0
mediapipe==0.10.9
scipy>=1.12.0

# ── Storage ─────────────────────────────────────────────
boto3>=1.34.0
